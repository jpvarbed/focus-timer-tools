{
  "name": "focus-timer-tools",
  "private": true,
  "type": "module",
  "workspaces": ["cli", "mcp"],
  "scripts": {
    "build": "bun run build:workspaces && bun run build:hosted",
    "build:workspaces": "bun run --filter '*' build",
    "build:hosted": "tsc -p tsconfig.hosted.json",
    "test": "bun test",
    "smoke": "bun scripts/smoke-memory.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "convex": "^1.17.4",
    "neo4j-driver": "^6.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
